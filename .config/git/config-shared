# Shared Git Configuration (Tracked)

[init]
	defaultBranch = main

[alias]
	amend = commit --amend -a
	c = "!f() { git switch -c \"$1\" origin/HEAD; }; f"
	fa = fetch --all --prune
	fp = "!git fa; git pull --ff-only"
	frk = !gh repo fork --remote-name=kevmoo --remote && gh repo set-default origin
	gp = grep -F
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[color]
	ui = auto

[url "git@github.com:"]
	insteadOf = https://github.com/
[hook "dart-format"]
	event = pre-commit
	command = ~/.config/git/hooks/dart_format_pre_commit.sh
