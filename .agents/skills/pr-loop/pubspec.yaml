name: pr_loop
description: Autonomous pull request review loop tools for GitHub PRs.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  args: ^2.4.0
  github_pr_triage:
    path: ../github-pr-triage
