# Fuzzy Finder (History, File search)
# fzf is pre-installed on Bluefin-DX
eval "$(fzf --bash)"

# Smart CD
eval "$(zoxide init bash)"

# Modern replacements
alias ls='eza --icons --git'
alias cat='bat'
